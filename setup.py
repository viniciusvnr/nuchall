from setuptools import setup, find_packages

if __name__ == '__main__':

    setup(
        name='nuchall',
        version='1.0.0',
        packages=find_packages(),
        url='',
        author_email=''
    )
